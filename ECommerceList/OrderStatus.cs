namespace ECommerceList
{
    /// <summary>
    /// Enum OrderStatus <see cref="OrderStatus" />  used to create data type for order status
    /// </summary>

    public enum OrderStatus
    {
        Default, Ordered, Cancelled
    }
}